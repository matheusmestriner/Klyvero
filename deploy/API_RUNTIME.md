# API runtime artifact

Split, base64-encoded API runtime source used by the deployment build. Source/manifests only; secrets, local environment files, databases, logs and security reports are excluded.
