'use client';import {FormEvent,useState} from 'react';import {api} from '../../lib/api';
export default function Setup(){const[msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api('/auth/bootstrap',{method:'POST',body:JSON.stringify(Object.fromEntries(f))});setMsg('Plataforma inicializada. Agora faça login.')}catch(e:any){setMsg(e.message||'Falha ao inicializar')}}return <div className="login"><form className="login-card" onSubmit={submit}><div className="logo"><i className="mark"/>Klyvero</div><h1>Inicializar plataforma</h1>{[['bootstrapToken','Token de inicialização'],['tenantName','Empresa'],['tenantSlug','Slug do workspace'],['name','Nome do administrador'],['email','E-mail'],['password','Senha (12+ caracteres)']].map(([k,l])=><div className="field" key={k}><label>{l}</label><input name={k} type={k==='password'?'password':k==='email'?'email':'text'} required/></div>)}<button className="btn primary full">Inicializar</button>{msg&&<div className="notice">{msg}</div>}</form></div>}
