import type { Metadata } from 'next';
export const metadata:Metadata={title:'Configuração inicial',robots:{index:false,follow:false,noarchive:true,nosnippet:true}};
export default function SetupLayout({children}:{children:React.ReactNode}){return children}
