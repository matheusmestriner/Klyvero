import {NextResponse} from 'next/server';export const dynamic='force-dynamic';export function GET(){const name=process.env.SITE_NAME||'Klyvero';const siteUrl=(process.env.SITE_URL||'http://localhost:3000').replace(/\/+$/,'');return new NextResponse(`# ${name}\n\n> AI Sales OS\n\n- Home: ${siteUrl}/\n- Robots: ${siteUrl}/robots.txt\n- Sitemap: ${siteUrl}/sitemap.xml\n`,{status:200,headers:{'content-type':'text/plain; charset=utf-8','cache-control':'public, max-age=3600'}})}
