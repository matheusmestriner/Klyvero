const BASE=process.env.NEXT_PUBLIC_API_URL??'http://localhost:4000/api/v1'; let token=''; export const session={set(v:string){token=v},get(){return token},clear(){token=''}};
export async function api(path:string,init:RequestInit={}){const r=await fetch(`${BASE}${path}`,{...init,credentials:'include',headers:{'content-type':'application/json',...(token?{authorization:`Bearer ${token}`}:{ } as any),...(init.headers??{})}});if(r.status===401&&path!='/auth/refresh'){const rr=await fetch(`${BASE}/auth/refresh`,{method:'POST',credentials:'include'});if(rr.ok){const d=await rr.json();token=d.accessToken;return api(path,init)}}if(!r.ok)throw new Error((await r.text())||`HTTP ${r.status}`);return r.status===204?null:r.json()}
